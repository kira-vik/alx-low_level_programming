#include "holberton.h"
/**
 * _abs - checks for alpha letters
 * Return: the absolute value of an integer
 * @c: number to be proccesed
 */
int _abs(int c)
{
	if (c < 0)
		return (-c);
	return (c);
}
